# Example split-tunnel: only route VPN subnet + selected nets through tunnel
# Copy structure from your issued full-tunnel conf; change AllowedIPs only.
# [Peer]
# AllowedIPs = 10.66.0.0/24, fd66:66:66::/64
