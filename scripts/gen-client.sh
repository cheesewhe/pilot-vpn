#!/usr/bin/env bash
# Placeholder: full client profile generation arrives with VPN backend (Stage 6) + API (Stage 7)
set -euo pipefail
echo "gen-client.sh: not implemented until VPN backend ADR is accepted (Stage 5–6)." >&2
echo "Use scripts/gen-keys.sh for raw key material." >&2
exit 2
